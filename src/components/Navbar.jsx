import LanguageSwitcher from './LanguageSwitcher';

const NavBar = () => {
  return (
    <nav>
      {/* other navigation links */}
      <LanguageSwitcher />
    </nav>
  );
}

export default NavBar;
